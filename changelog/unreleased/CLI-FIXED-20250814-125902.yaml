component: CLI
kind: FIXED
body: Fixed a bug where create commands with `--update` flag would misbehave on API
  error
time: 2025-08-14T12:59:02.190331622Z
custom:
  DETAILS: In some cases, like a 5xx from the API, the CLI would try to update a non-existent
    resource and fail to report the correct error message.
  PR: "382"
  TYPE: CLI
